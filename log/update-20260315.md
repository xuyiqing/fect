# 2026-03-15

> Branch: `cfe` | 21 commits (`be7bbcb`..`fd2abd3`) | Profile: r-package

## What Changed

Full-day effort on the `cfe` branch covering the entire `factors.from` refactoring, CFE bifurcation, bootstrap tests, CV fix, and Quarto book update.

### factors.from Refactoring (Phases 1-7)

1. **Phase 1 — `factors.from` parameter** (`53b461d`, `cafb66d`): Added `factors.from = "notyettreated" | "nevertreated"` to all 3 fect signatures. Built II matrix filtering: when nevertreated, zero out all non-never-treated columns. Added validation guards and output storage.

2. **Phase 2 — gsynth auto-override** (`37d516b`): `method="gsynth"` auto-sets `factors.from="nevertreated"`. Added `em` parameter. Rerouted `ife+nevertreated` through `fect_gsynth`.

3. **Phase 3 — Parametric bootstrap** (`070d574`): Unlocked parametric bootstrap for ife/fe/cfe (was gsynth-only). Fixed `draw.error()` method-aware dispatch.

4. **Phase 4 — Remove deprecated methods** (`953da8a`, `efa2af0`): Removed `method="polynomial"`, `"bspline"`, `"cfe_old"` entirely.

5. **Phase 5 — Input validation** (`cafb66d`): Guards for invalid `factors.from` values, `mc/both+nevertreated`, `em+notyettreated`, insufficient never-treated units.

6. **Phase 6 — em parameter + reroute** (`37d516b`): `em=TRUE/FALSE` controls EM algorithm. `ife+nevertreated` → `fect_gsynth()`.

7. **Phase 7 — Rename** (`fa9baf2`): `fect_gsynth` → `fect_nevertreated` (function and file).

### Phase 3a — CFE Bifurcation

8. **CFE in fect_nevertreated** (`c0cab63`): `method="cfe"` + `factors.from="nevertreated"` now routes through `fect_nevertreated` using `complex_fe_ub`. Added: CFE array subsetting (5 arrays), Type A/B extra FE classification, three-layer projection, block coordinate descent for unit-specific parameters, 7 helper functions. 36 new tests (categories A-H), 92/92 pass.

### Bootstrap Tests, CV Fix, r=0 Invariance

9. **Category I bootstrap tests** (`5136a03`): Added tests I9-I11 — `quantile.CI=TRUE` CIs, `em` no-op confirmation, unbalanced data (5% dropped) forcing `_ub`/EM path. Fixed bug: `est.att.avg` → `est.avg`.

10. **r=0 invariance test** (`e5f098e`): Test I12 — `factors.from` is no-op at r=0. Divergence ~0.003-0.007 is structural (different solvers), tolerance 1e-2.

11. **CV routing fix** (`4cfe25c`): `fect_cv` delegates `cfe+nevertreated` to `fect_nevertreated(..., method="cfe", CV=TRUE)`, mirroring gsynth pattern. Fixed `unname()` bug. Discovered 4th call site in `default.R` line ~1963.

### Quarto Book Update

12. **Documentation overhaul** (`219510d`, `0fa790c`, `fd2abd3`): "Two Research Settings" (synth vs DID), `factors.from` docs, estimation vs inference callouts, `esplot()` and `fect_mspe()` sections. Chapter reorder: Welcome → Start → Fect → CFE → Plots → Gsynth → Panel → Sens. Dead link cleanup, typo fixes. 10/10 chapters render.

## Files Changed

| File | Action | Description |
|------|--------|-------------|
| `R/default.R` | modified | factors.from/em params, validation, II matrix filtering, dispatch routing, CV=FALSE relaxation, param forwarding |
| `R/fect_nevertreated.R` | created (renamed) | Renamed from fect_gsynth.R; added CFE bifurcation, three-layer projection, BCD, 7 helpers, unname() fix |
| `R/boot.R` | modified | factors.from param, draw.error() dispatch for ife/cfe+nevertreated, CFE array subsetting, param forwarding |
| `R/cv.R` | modified | factors.from+CFE params in signature, cfe+nevertreated delegation block |
| `R/fe.R` | modified | Added eff.tr to return list |
| `R/cfe.R` | modified | Added eff.tr to return list |
| `R/fect_gsynth.R` | deleted | Renamed to fect_nevertreated.R |
| `tests/testthat/test-factors-from-refactor.R` | created | 1714 lines, 131 tests: phases 1-6, categories A-H, I1-I12, CV routing test |
| `vignettes/index.qmd` | modified | Two Research Settings, Estimator Families, chapter reorder |
| `vignettes/02-fect.Rmd` | modified | factors.from/em callouts, estimation vs inference separation, typo fixes |
| `vignettes/04-gsynth.Rmd` | modified | Equivalence callout, "When to use" section (original narrative preserved) |
| `vignettes/07-cfe.Rmd` | modified | CFE+nevertreated section, fect_mspe() example |
| `vignettes/03-plots.Rmd` | modified | esplot() section with 3 examples |
| `vignettes/aa-cheatsheet.Rmd` | modified | factors.from/em rows, utility functions section |
| `vignettes/01-start.Rmd` | modified | Dataset description update for nevertreated |
| `vignettes/05-panel.Rmd` | modified | Removed dead R script link |
| `vignettes/06-sens.Rmd` | modified | Removed dead R script link |
| `vignettes/_quarto.yml` | modified | Chapter reorder |
| `log/HANDOFF-factors-from.md` | modified | Comprehensive update |

## Design Decisions

1. **est.att.avg → est.avg**: The fect output field for aggregate ATT with CI is `est.avg` (from `fect_boot()` at boot.R line 4653), not `est.att.avg`. The old I8 test and new I9 test both referenced the wrong name. Discovered via auditor BLOCK.

2. **r=0 tolerance 1e-2 not 1e-3**: At r=0, notyettreated (via `inter_fe_ub`) and nevertreated (via `inter_fe`) use different solvers with ~0.003-0.007 ATT divergence. This is structural (different estimation samples), not a bug. Increasing N to 500 shrinks the gap to ~0.0006 but it doesn't monotonically vanish.

3. **CV delegation pattern for cfe+nevertreated**: Mirrored the existing gsynth delegation in `fect_cv` (lines 177-191) rather than adding full CFE CV logic to `fect_cv`. This reuses the already-tested CV loop in `fect_nevertreated` (lines 679-879). Builder discovered a 4th call site (`default.R` line ~1963, the `se=FALSE+CV=TRUE` path) that also needed param forwarding — this was not in the original spec.

4. **Quarto book "Two Research Settings" framing**: User directed framing as two research settings (synth vs DID) rather than "two estimation regimes". The synth setting uses never-treated controls to learn time effects (gsynth origin); the DID setting generalizes to EM imputation. This bifurcation also affects inference (parametric vs nonparametric bootstrap).

5. **Preserve gsynth narrative**: User explicitly requested keeping the original gsynth chapter narrative and IFE/MC comparison examples. New content (equivalence callout, "When to use" section) was added after the existing intro, not as a replacement.

6. **em is no-op for nevertreated**: Removed `em=FALSE` from all gsynth equivalence statements in the book. Test I10 confirms em has zero effect under nevertreated — `fect_nevertreated` doesn't accept em, and the C++ solvers don't take it.

7. **Chapter reorder**: CFE moved right after Fect Main Program (same IFE family), Gsynth moved after Plots (it's a specific research setting, not a core method).

## Handoff Notes

- ~~**CFE CV r-selection issue**~~: RESOLVED — test misspecification (missing `Z="Z"`). See `log/2026-03-16-cfe-cv-rselect-fix.md`.
- ~~**cv.R vs fect_mspe.R**~~: RESOLVED — unified via `.score_residuals()` (Phase 1) and `cv.method` (Phase 2). `fect_mspe_sim` deleted.
- ~~**Test gaps from book review**~~: RESOLVED — 53 utility tests added (`5460b13`).
- **Phase 3b** (merge IFE into CFE): still open. Requires verifying `complex_fe_ub` with empty CFE ≡ `inter_fe_ub`.
- **fect_mspe + CV=TRUE bug**: discovered during Phase 2 testing. `fect_mspe()` errors on CV-fitted models.

### 2026-03-16 follow-up work

All committed and pushed to `cfe`:

| Commit | What |
|--------|------|
| `caa456f` | Score unification Phase 1: `.score_residuals()` shared scoring |
| `eac0187` | Phase 2: `cv.method` unification, fect_mspe simplification, 1% rule |
| `cea6966` | cv.sample k-fold CV (all_units + treated_units) + parallel auto-threshold |

259/259 tests pass. See `HANDOFF-factors-from.md` for current state.

## Verification

- **devtools::test(filter = "factors-from-refactor")**: 131/131 pass, 0 failures, 0 warnings
- **quarto render**: 10/10 chapters render, `_book/index.html` produced
- **Review verdicts**: All 4 runs passed skeptic review (3× PASS, 1× PASS WITH NOTE)
